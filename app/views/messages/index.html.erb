Create Direct Message <%= link_to 'home', user_path(@user) %>

<div id="search_form">
    <%= form_tag '/users', :method => :get do %>
        <%= text_field_tag :search, params[:search] %>
        <%= submit_tag "Search" %>
    <% end %>
</div><br>



Show all the contacts here

<ul>
    <% User.all.each do |user| %>
        <li><%= link_to user.name, rooms_path(:contact_id => user.id), :method => :post %></li>
    <% end %>
</ul>