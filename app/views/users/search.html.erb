<div id="search-page">
  <span class="home-link"><%= link_to 'Home', home_path %></span><br>

  <%= text_field_tag :search, nil, placeholder: 'search...', onkeyup: 'filterUsers();' %>

  <ul id="usersList">
    <% User.all.each do |u| %>
      <li><%= link_to u.name, rooms_path(user_name: u.name), method: :post %></li>
    <% end %>
  </ul> 
</div>

  
